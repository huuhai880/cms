SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
ALTER PROCEDURE [dbo].[MD_PRODUCT_ATTRIBUTES_Delete_AdminWeb]
    @PRODUCTID BIGINT = 0,
    @DELETEDUSER VARCHAR(50) = NULL
AS
BEGIN

    INSERT INTO MD_PRODUCT_ATTRIBUTES_LOG
    SELECT 
           *,
           @DELETEDUSER,
           GETDATE()
    FROM   MD_PRODUCT_ATTRIBUTES
    WHERE  MD_PRODUCT_ATTRIBUTES.PRODUCTID = @PRODUCTID

    -- UPDATE  MD_PRODUCT_ATTRIBUTES
    -- SET     ISDELETED = 1,
    --         DELETEDUSER = @DELETEDUSER,
    --         DELETEDDATE = GETDATE()
    -- WHERE   PRODUCTID = @PRODUCTID

    DELETE MD_PRODUCT_ATTRIBUTES
    WHERE  PRODUCTID = @PRODUCTID;
END
GO
