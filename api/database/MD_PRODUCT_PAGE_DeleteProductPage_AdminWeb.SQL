SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
ALTER PROCEDURE [dbo].[MD_PRODUCT_PAGE_DeleteProductPage_AdminWeb]
    @PRODUCTID BIGINT,
    @DELETEDUSER AS nvarchar(100) = NULL
AS
BEGIN
    INSERT INTO MD_PRODUCT_PAGE_LOG
    SELECT *,
           @DELETEDUSER,
           GETDATE()
    FROM    MD_PRODUCT_PAGE
    WHERE   PRODUCTID = @PRODUCTID;

	-- UPDATE [dbo].[MD_PRODUCT_PAGE]
	-- SET MD_PRODUCT_PAGE.ISDELETED= 1,
	-- MD_PRODUCT_PAGE.UPDATEDUSER=@DELETEDUSER,
	-- MD_PRODUCT_PAGE.UPDATEDDATE=GETDATE()
	-- WHERE MD_PRODUCT_PAGE.PRODUCTID=@PRODUCTID

    DELETE MD_PRODUCT_PAGE
    WHERE  PRODUCTID = @PRODUCTID;

END
GO
